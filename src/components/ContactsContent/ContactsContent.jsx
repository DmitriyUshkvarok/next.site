import styles from './contactsContent.module.css';

const ContactsContent = () => {
  return (
    <section>
      <div className={styles.contactsWrapper}></div>
    </section>
  );
};

export default ContactsContent;
